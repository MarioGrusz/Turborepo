{
    "$schema": "https://turborepo.org/schema.json",
    "pipeline": {
       "dev": { "cache": false },
       "test": {},
       "build": {
          "dependsOn": ["^build"],
          "outputs": ["dist/**"]
       }
    }
 }